# 計程車檢舉平台 — 專案規格書

## 0. 專案定位

一個讓乘客可以「查詢特定計程車車牌的過往評價」與「提交檢舉」的網站。
**核心設計原則：查詢制，不做排行榜/瀏覽清單** — 使用者必須輸入完整車牌才能看到資料，避免整站變成可被搜尋引擎索引的公開黑名單，降低法律爭議與濫用風險。

法規依據（供網站「關於本站」頁面引用）：
- 《道路交通管理處罰條例》第38條：計程車駕駛人任意拒載乘客或故意繞道行駛者，處新臺幣600–1200元罰鍰。
- 官方申訴（1999、警局、公共運輸處）**不強制要求影片佐證**，有錄音/影像佐證更有利，但沒有也可申訴。
- 本平台為「民眾自主分享資訊」性質，**不是政府認定結果**，此聲明必須明顯呈現在網站上（法律免責考量）。

---

## 1. 核心功能

### 1.1 車牌查詢（首頁主功能）
- 首頁只有一個大大的搜尋框：輸入完整車牌 → 查詢
- 查詢結果顯示：
  - 該車牌完整顯示（依你的決定，不遮蔽）
  - 兩組統計數字分開呈現，例如：
    - 🔓 匿名檢舉：5 則
    - ✅ 記名檢舉（Email 驗證）：10 則
  - 兩類檢舉的內容列表也要**分開呈現**（不要混在同一串列表），並清楚標示來源類型，讓使用者自行判斷可信度
  - 每則檢舉顯示：時間、事由分類、文字說明、佐證縮圖（若有上傳）
- 若查無資料：顯示「目前尚無相關檢舉紀錄」，並附上「填寫檢舉」按鈕

### 1.2 提交檢舉
共用欄位：
- 車牌號碼（文字輸入，需正規化格式，去除大小寫與符號差異後比對，例如 `ABC-1234` 與 `abc1234` 視為同一筆）
- 事由分類（**下拉選單**，非自由填空，避免情緒性用詞與方便統計）：
  - 拒載
  - 繞路
  - 超收車資 / 亂跳表
  - 服務態度不佳
  - 危險駕駛
  - 其他
- 文字描述（限制字數，如 300 字，避免長篇謾罵）
- 佐證照片/影片上傳（**選填**）
- 發生時間、地點（選填）

**兩種提交模式：**

| | 匿名模式 | 記名模式 |
|---|---|---|
| 門檻 | 無，直接填寫送出 | 需輸入 Email → 收驗證碼 → 輸入驗證碼通過才能送出 |
| 顯示標籤 | 🔓 匿名 | ✅ Email 已驗證 |
| 防濫用 | 靠 IP/裝置指紋限制頻率 | 靠 Email 限制頻率（同 Email 對同車牌 24 小時內限 1 次） |
| 儲存 Email | 不儲存 | 儲存於後端（**不公開顯示**，僅供防濫用與未來申訴查證使用） |

### 1.3 佐證上傳（選填）
- 使用者可選擇上傳 1 張照片或 1 段短影片
- 檔案上傳至 Google Drive 指定資料夾，Sheets 只存檔案連結
- 前端限制檔案大小（建議圖片 <5MB、影片 <30MB）避免 Apps Script 逾時

### 1.4 申訴/移除機制（建議加入，保護平台與司機雙方）
這點你原本沒特別要求，但**強烈建議加入**，理由是純粹用來降低法律風險：
- 提供一個「這則檢舉是我本人車輛，內容不實」的申訴表單（司機端）
- 後台管理者可下架不當內容
- 如果你希望先做最簡 MVP，這功能可以列為 Phase 2，但網站上的免責聲明一定要在 MVP 就寫清楚

---

## 2. 技術架構

考量你希望用 GitHub + Google Sheets，且不想額外架伺服器，建議：

```
前端 (GitHub Pages / Vercel 靜態部署)
  │
  │  fetch API 呼叫
  ▼
Google Apps Script Web App（免費後端）
  │
  ├── 讀寫 Google Sheets（資料庫）
  ├── GmailApp 寄送 Email 驗證碼（記名模式用）
  └── 上傳檔案存至 Google Drive（佐證用）
```

**為什麼用 Google Apps Script 當後端：**
- 不用另外租主機或設定 serverless function
- 天生就能操作 Google Sheets 與 Gmail，符合你「同事用 Sheets 存資料」的需求
- 部署成「Web App」後可取得一組 URL，前端直接打 API 即可

**已知限制（先讓你有心理準備）：**
- Google Sheets API 有速率限制（每帳號每100秒約60–300次請求），小規模夠用，若爆紅流量大需考慮換 Firebase/Supabase
- Apps Script 單次執行有時間上限（約6分鐘），大檔案上傳要注意
- 這個架構適合 MVP 驗證構想，不適合當作長期承受高流量的正式後端

---

## 3. 資料表設計（Google Sheets）

### Sheet 1: `Reports`（檢舉紀錄）

| 欄位 | 型態 | 說明 |
|---|---|---|
| id | string | 唯一識別碼（時間戳+隨機碼） |
| plate_normalized | string | 正規化後的車牌（去符號、轉大寫），用於查詢比對 |
| plate_display | string | 使用者輸入的原始車牌格式，用於顯示 |
| type | string | `anonymous` 或 `verified` |
| category | string | 拒載/繞路/超收車資/服務態度不佳/危險駕駛/其他 |
| description | string | 文字說明（限300字） |
| evidence_url | string | 佐證檔案連結（Google Drive，選填，可空） |
| occurred_at | string | 發生時間（選填） |
| location | string | 發生地點（選填） |
| submitted_at | timestamp | 提交時間 |
| submitter_hash | string | 匿名模式存裝置/IP hash；記名模式存 Email hash（用於防濫用頻率限制，**不直接存明碼 Email**） |
| status | string | `active` / `removed`（供後台審核使用） |

### Sheet 2: `EmailVerifications`（記名模式驗證用，暫存）

| 欄位 | 型態 | 說明 |
|---|---|---|
| email | string | 使用者 Email |
| otp_code | string | 6位數驗證碼 |
| created_at | timestamp | 產生時間 |
| expires_at | timestamp | 過期時間（建議10分鐘） |
| verified | boolean | 是否已驗證通過 |

> 這張表的資料應定期清除過期紀錄（Apps Script 可設定時間觸發器每日清理）。

---

## 4. API 設計（Apps Script Web App 端點，用 `action` 參數區分）

- `POST ?action=searchPlate` → 輸入車牌，回傳該車牌所有 `active` 狀態的檢舉列表（分匿名/記名兩組回傳）
- `POST ?action=submitAnonymousReport` → 送出匿名檢舉
- `POST ?action=sendOtp` → 輸入 Email，產生驗證碼並寄信
- `POST ?action=verifyOtp` → 驗證碼比對，通過後回傳一次性 token
- `POST ?action=submitVerifiedReport` → 帶著上一步 token 送出記名檢舉
- `POST ?action=uploadEvidence` → 上傳檔案至 Drive，回傳連結（給前面兩個 submit 動作使用）

---

## 5. 防濫用機制

- 車牌格式正規化比對，避免同一車牌因格式不同被拆成多筆查不到
- 匿名模式：以瀏覽器指紋或 localStorage token + 後端記錄的 hash，限制「同裝置對同車牌」24小時內僅能提交1次
- 記名模式：以 Email hash 限制「同 Email 對同車牌」24小時內僅能提交1次；同 Email 每日總提交次數上限（例如5筆），避免驗證後仍濫用
- 文字內容長度限制 + 前端基本髒話/人身攻擊字詞過濾（可選配）
- 後台管理者可將任何一筆標記為 `removed`

---

## 6. 頁面規劃（Information Architecture，手機優先）

**首頁主標語（Slogan）：** 免舉證，出口氣
（放在首頁最上方最大字，搜尋框緊接在下方，一眼就能抓到「不用像官方申訴那樣舉證」這個核心賣點）

1. **首頁**：主標語 + 搜尋框（唯一主要入口）+ 簡短說明本站性質與免責聲明
2. **查詢結果頁**：車牌資訊、統計數字、匿名/記名列表分區、底部「填寫檢舉」按鈕
3. **填寫檢舉頁**：車牌輸入 → 選擇匿名/記名 → 若記名則跳出 Email + OTP 流程 → 填寫事由/描述/佐證 → 送出
4. **關於本站 / 免責聲明頁**：說明本站性質、法規依據、隱私權政策、申訴聯絡方式
5.（Phase 2）**司機申訴頁**：讓被檢舉者提出異議

---

## 7. UI/UX 原則

- 手機優先，單欄版面，避免任何需要橫向捲動的元件
- 首頁搜尋框要夠大、夠明顯，一進站就知道怎麼用
- 顏色語意化：匿名用中性色（如灰/藍），記名驗證用強調色（如綠色勾勾），讓使用者一眼分辨可信度差異
- 表單分步驟呈現（一次只問一件事），避免長表單嚇跑手機使用者
- 全站需有清楚、不刺眼的免責聲明橫幅或頁尾

---

## 8. MVP 範圍建議

**Phase 1（先做這些就能上線驗證）：**
- 車牌查詢
- 匿名檢舉提交（含事由分類、描述、選填佐證上傳）
- 記名檢舉提交（Email OTP 驗證）
- 基本防濫用（頻率限制）
- 免責聲明頁

**Phase 2（驗證有人用之後再加）：**
- 司機申訴/移除機制
- 後台管理介面
- 髒話過濾、更完整的內容審核
